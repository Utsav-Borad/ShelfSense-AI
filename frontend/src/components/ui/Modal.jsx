import { AnimatePresence, motion } from 'framer-motion';
export default function Modal({ open, onClose, title, children }) { return <AnimatePresence>{open && <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><motion.div className="app-modal" role="dialog" aria-modal="true" aria-label={title} initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: .96 }} onMouseDown={(e) => e.stopPropagation()}><div className="modal-title"><h3>{title}</h3><button className="icon-btn" onClick={onClose} aria-label="Close dialog"><i className="bi bi-x-lg" /></button></div>{children}</motion.div></div>}</AnimatePresence>; }
