export default function Pagination({ page = 1, totalPages = 1, onChange }) { return <nav aria-label="Pagination"><ul className="pagination app-pagination"><li><button disabled={page === 1} onClick={() => onChange?.(page - 1)} aria-label="Previous page">‹</button></li><li><span>{page} / {totalPages}</span></li><li><button disabled={page === totalPages} onClick={() => onChange?.(page + 1)} aria-label="Next page">›</button></li></ul></nav>; }
